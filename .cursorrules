# 收银系统开发规则

## 📱 响应式设计规则 (必须遵循)

### 设备适配
- **主要目标设备**: iPad (1024x768, 768x1024)
- **支持竖屏操作**: 必须适配竖屏模式 `@media (orientation: portrait)`
- **有效利用空间**: 避免过大的UI元素，优化空间利用率

### 弹窗和对话框
- **最大宽度**: 800px，小屏幕时使用 95% 宽度
- **最大高度**: 90vh，超出时启用垂直滚动
- **边距**: 顶部边距不超过 2vh，小屏幕时为 1vh
- **内边距**: 统一使用 15px，避免过大的间距

### 表单和输入控件
- **输入框高度**: 38-44px (非 60px+)
- **字体大小**: 12-16px，避免过大字体
- **按钮尺寸**: 高度 38-42px，宽度根据内容适配
- **间距**: 元素间距 15-20px，避免过大空隙

### 键盘组件
- **按键大小**: 42-44px 高度，适中宽度
- **间距**: 6-8px 间隙
- **字体**: 14-18px，确保清晰可读
- **响应式**: 竖屏时适当缩小尺寸

### 媒体查询规则
```less
// 竖屏适配
@media (orientation: portrait) {
  .dialog { width: 95% !important; }
  .form-layout { flex-direction: column; }
}

// 小屏幕适配  
@media (max-width: 900px) {
  .component { width: 95% !important; }
}
```

## 🎨 UI设计原则

### 颜色和视觉
- **主色调**: #409eff (蓝色)
- **成功色**: #67c23a
- **警告色**: #e6a23c  
- **危险色**: #f56c6c
- **圆角**: 6-8px，避免过大圆角

### 组件尺寸
- **紧凑布局**: 优先使用 small/medium 尺寸
- **列表项**: 高度 40-50px
- **卡片**: 内边距 15px
- **表格**: 行高 44px

### 交互设计
- **点击目标**: 最小 44x44px (符合触摸标准)
- **悬浮效果**: 1-2px 位移，避免过度动画
- **过渡时间**: 0.2-0.3s，保持流畅

## 🚫 禁止事项

### UI禁忌
- ❌ 弹窗宽度超过 90%
- ❌ 按钮高度超过 50px  
- ❌ 字体大小超过 20px (标题除外)
- ❌ 内边距超过 25px
- ❌ 忽略竖屏适配
- ❌ 固定像素尺寸 (应使用响应式)

### 性能禁忌
- ❌ 复杂的CSS动画
- ❌ 过多的DOM元素
- ❌ 不必要的深层嵌套

## 🔧 开发流程

### 新组件开发
1. **移动优先**: 先设计小屏幕版本
2. **测试竖屏**: 确保竖屏模式正常
3. **适配检查**: 在不同尺寸下测试
4. **代码审查**: 确保符合本规则

### 现有组件修改
1. **评估影响**: 检查响应式兼容性
2. **测试覆盖**: iPad横屏/竖屏测试
3. **渐进增强**: 保持向后兼容

## 📝 代码规范

### CSS/LESS
```less
// ✅ 推荐
.component {
  width: 800px;
  max-width: 95%;
  height: auto;
  padding: 15px;
  
  @media (orientation: portrait) {
    width: 95%;
  }
}

// ❌ 禁止
.component {
  width: 1200px;
  height: 800px; 
  padding: 30px;
}
```

### Vue组件
- 使用 `scoped` 样式
- 添加响应式媒体查询
- 测试不同设备尺寸
- 文档说明支持的设备

## 📋 检查清单

开发完成后必须检查：
- [ ] iPad横屏显示正常
- [ ] iPad竖屏显示正常  
- [ ] 弹窗不超出屏幕
- [ ] 按钮大小适合触摸
- [ ] 文字清晰可读
- [ ] 操作流畅不卡顿
- [ ] 适配不同分辨率

## 🎯 性能目标

- **首屏渲染**: < 2秒
- **操作响应**: < 200ms
- **内存使用**: < 100MB
- **CPU占用**: < 30%

严格遵循以上规则，确保收银系统在iPad设备上提供优质的用户体验！ 