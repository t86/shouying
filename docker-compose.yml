version: '3'

services:
  web:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./dist:/usr/share/nginx/html/shouyin
      - ./nginx.conf:/etc/nginx/nginx.conf
    environment:
      - NODE_ENV=production
