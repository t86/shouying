name: shouyin

services:
  web:
    build: .
    ports:
      - "8080:8080"
      - "8081:8081"
    volumes:
      - ./dist:/usr/share/nginx/html/shouyin
      - ./nginx.conf:/etc/nginx/nginx.conf
    environment:
      - NODE_ENV=production
